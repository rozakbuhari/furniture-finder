import React from 'react';
import { css } from '@emotion/core';

const ProductsPage = () => {
  return (
    <div
      css={css`
        color: yellowgreen;
      `}>
      <h2>Product List</h2>
    </div>
  );
};

export default ProductsPage;
