import React from 'react';
import { css } from '@emotion/core';

const SearchPage = () => {
  return (
    <div
      css={css`
        color: yellowgreen;
      `}>
      <h2>Search Page</h2>
    </div>
  );
};

export default SearchPage;
